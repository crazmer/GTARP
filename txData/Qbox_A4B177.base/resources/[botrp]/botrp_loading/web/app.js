const app=document.getElementById('app');const title=document.getElementById('title');const subtitle=document.getElementById('subtitle');const status=document.getElementById('status');const bar=document.getElementById('bar');const percent=document.getElementById('percent');
let progress=0;
function setProgress(value){const next=Math.max(progress,Math.round(Math.max(0,Math.min(1,Number(value)||0))*100));progress=next;if(bar)bar.style.width=next+'%';if(percent)percent.textContent=next+'%';if(status)status.textContent=next>=98?'READY':next>=70?'LOADING RESOURCES':next>=30?'LOADING WORLD':'CONNECTING TO SERVER';for(const [id,threshold] of [['step2',30],['step3',65],['step4',98]]){const el=document.getElementById(id);if(el&&next>=threshold)el.classList.add('active')}}
window.addEventListener('message',e=>{const d=e.data||{};if(d.eventName==='loadProgress'){setProgress(d.loadFraction);return}if(d.action==='show'){title.textContent=d.title||'Welcome to BotRP';subtitle.textContent=d.subtitle||'Your next story starts here.';status.textContent='LOADING';app.classList.remove('hidden');return}if(d.action==='hide'){app.classList.add('hidden')}});
setProgress(0.01);
